import DashboardLayout, { type DashboardMenuItem } from "@/components/DashboardLayout";
import { trpc } from "@/lib/trpc";
import { Link2, Network, UserRoundCheck, UsersRound } from "lucide-react";

const menu: DashboardMenuItem[] = [
  { icon: Network, label: "Minha rede direta", path: "/membros/rede", group: "Minha operação" },
  { icon: UserRoundCheck, label: "Meu apresentador", path: "/membros/patrocinador", group: "Minha operação" },
  { icon: Link2, label: "Preparar convites", path: "/membros/convites", group: "Relacionamento" },
];

export default function MemberReferrals() {
  const network = trpc.member.referrals.useQuery();
  const data = network.data;
  return <DashboardLayout menuItems={menu} title="Escritório Virtual"><main className="mx-auto w-full max-w-6xl space-y-7 p-5 sm:p-8"><header className="space-y-2"><span className="text-xs uppercase tracking-[0.16em] text-emerald-300">Minha operação</span><h1 className="text-3xl font-semibold text-white">Minha rede direta</h1><p className="max-w-3xl text-sm leading-6 text-zinc-300">Consulte o apresentador associado e os membros vinculados diretamente à sua conta. Esta visão exibe somente relações registradas na sua própria rede.</p></header>{network.isLoading ? <p className="text-sm text-zinc-400">Carregando vínculos...</p> : <><section className="grid gap-5 md:grid-cols-3"><article className="rounded-2xl border border-emerald-300/25 bg-emerald-300/10 p-5"><p className="text-xs uppercase tracking-wider text-emerald-200">Vínculos ativos</p><strong className="mt-2 block text-3xl text-white">{data?.activeCount ?? 0}</strong><p className="mt-2 text-sm text-zinc-300">Indicações diretas ativas.</p></article><article className="rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><p className="text-xs uppercase tracking-wider text-zinc-400">Vínculos arquivados</p><strong className="mt-2 block text-3xl text-white">{data?.archivedCount ?? 0}</strong><p className="mt-2 text-sm text-zinc-400">Histórico sem exposição pública.</p></article><article className="rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><p className="text-xs uppercase tracking-wider text-zinc-400">Apresentador</p><strong className="mt-2 block text-lg text-white">{data?.sponsor?.name || "Não definido"}</strong><p className="mt-2 text-sm text-zinc-400">Vínculo configurado pela administração.</p></article></section><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><article className="rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><div className="flex items-center gap-2 text-white"><UserRoundCheck className="size-5 text-emerald-300" /><h2 className="font-medium">Meu apresentador</h2></div>{data?.sponsor ? <div className="mt-4 rounded-xl border border-white/10 bg-black/25 p-4"><p className="font-medium text-white">{data.sponsor.name || "Membro da Página Lucrativa"}</p><p className="mt-1 text-sm text-zinc-400">Relação ativa desde {new Date(data.sponsor.createdAt).toLocaleDateString("pt-BR")}.</p></div> : <p className="mt-4 text-sm leading-6 text-zinc-400">Ainda não há apresentador vinculado ao seu perfil. Caso precise de ajuste, abra uma solicitação no Escritório Virtual.</p>}</article><section className="rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><div className="flex items-center gap-2 text-white"><UsersRound className="size-5 text-emerald-300" /><h2 className="font-medium">Indicações diretas</h2></div>{data?.referrals?.length ? <div className="mt-4 space-y-3">{data.referrals.map(referral => <article key={referral.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-white/10 bg-black/25 p-4"><div><p className="font-medium text-white">{referral.name || "Membro da Página Lucrativa"}</p><p className="mt-1 text-xs text-zinc-500">Vinculado em {new Date(referral.createdAt).toLocaleDateString("pt-BR")}</p></div><span className={referral.status === "active" ? "rounded-full bg-emerald-400/10 px-3 py-1 text-xs text-emerald-200" : "rounded-full bg-zinc-700/50 px-3 py-1 text-xs text-zinc-300"}>{referral.status === "active" ? "Ativo" : "Arquivado"}</span></article>)}</div> : <p className="mt-4 rounded-xl border border-dashed border-white/15 p-5 text-sm leading-6 text-zinc-400">Nenhuma indicação direta foi vinculada à sua conta até o momento. Os vínculos são administrados com critérios de privacidade e rastreabilidade.</p>}</section></section></>}</main></DashboardLayout>;
}
