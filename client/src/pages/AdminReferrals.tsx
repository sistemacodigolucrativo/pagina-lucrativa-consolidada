import DashboardLayout, { type DashboardMenuItem } from "@/components/DashboardLayout";
import { trpc } from "@/lib/trpc";
import { Network, ShieldCheck, UserPlus, UsersRound } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

const menu: DashboardMenuItem[] = [
  { icon: Network, label: "Visão geral", path: "/admin", group: "Administração" },
  { icon: UsersRound, label: "Membros e rede", path: "/admin/membros", group: "Administração" },
];

export default function AdminReferrals() {
  const utils = trpc.useUtils();
  const members = trpc.admin.referralMembers.useQuery();
  const links = trpc.admin.referralLinks.useQuery();
  const [sponsorId, setSponsorId] = useState("");
  const [referredUserId, setReferredUserId] = useState("");
  const [status, setStatus] = useState<"active" | "archived">("active");
  const save = trpc.admin.setReferralLink.useMutation({ onSuccess: () => { void utils.admin.referralLinks.invalidate(); setReferredUserId(""); toast.success("Vínculo de indicação salvo."); }, onError: error => toast.error(error.message) });
  function submit(event: FormEvent) { event.preventDefault(); const sponsor = Number(sponsorId); const referred = Number(referredUserId); if (!Number.isInteger(sponsor) || !Number.isInteger(referred) || sponsor <= 0 || referred <= 0) return toast.error("Selecione patrocinador e indicado."); if (sponsor === referred) return toast.error("Um membro não pode patrocinar a si mesmo."); save.mutate({ sponsorId: sponsor, referredUserId: referred, status }); }
  return <DashboardLayout menuItems={menu} title="Administração"><main className="mx-auto w-full max-w-7xl space-y-7 p-5 sm:p-8"><header className="space-y-2"><span className="text-xs uppercase tracking-[0.16em] text-amber-300">Governança de relacionamento</span><h1 className="text-3xl font-semibold text-white">Membros e rede</h1><p className="max-w-3xl text-sm leading-6 text-zinc-300">Crie, atualize ou arquive vínculos de patrocínio. O membro vê somente seu patrocinador e seus próprios indicados diretos.</p></header><section className="grid gap-6 xl:grid-cols-[0.85fr_1.15fr]"><form onSubmit={submit} className="space-y-4 rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><div className="flex items-center gap-2 text-white"><UserPlus className="size-5 text-amber-300" /><h2 className="font-medium">Vincular indicação</h2></div><label className="block text-sm text-zinc-200">Patrocinador<select required value={sponsorId} onChange={event => setSponsorId(event.target.value)} className="mt-1 w-full rounded-lg border border-white/15 bg-black px-3 py-2 text-white"><option value="">Selecione</option>{members.data?.map(member => <option key={member.id} value={member.id}>{member.name || member.email || `Membro #${member.id}`}</option>)}</select></label><label className="block text-sm text-zinc-200">Indicado<select required value={referredUserId} onChange={event => setReferredUserId(event.target.value)} className="mt-1 w-full rounded-lg border border-white/15 bg-black px-3 py-2 text-white"><option value="">Selecione</option>{members.data?.map(member => <option key={member.id} value={member.id}>{member.name || member.email || `Membro #${member.id}`}</option>)}</select></label><label className="block text-sm text-zinc-200">Estado<select value={status} onChange={event => setStatus(event.target.value as "active" | "archived")} className="mt-1 w-full rounded-lg border border-white/15 bg-black px-3 py-2 text-white"><option value="active">Ativo</option><option value="archived">Arquivado</option></select></label><button disabled={save.isPending} className="inline-flex items-center gap-2 rounded-lg bg-amber-300 px-4 py-2 text-sm font-semibold text-black disabled:opacity-60">{save.isPending ? "Salvando..." : "Salvar vínculo"}</button></form><section className="rounded-2xl border border-white/10 bg-zinc-950/60 p-5"><div className="flex items-center gap-2 text-white"><ShieldCheck className="size-5 text-amber-300" /><h2 className="font-medium">Vínculos cadastrados</h2></div>{links.isLoading ? <p className="mt-4 text-sm text-zinc-400">Carregando vínculos...</p> : links.data?.length ? <div className="mt-4 space-y-3">{links.data.map(link => <article key={link.id} className="rounded-xl border border-white/10 bg-black/25 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm text-zinc-400">Patrocinador</p><p className="font-medium text-white">{link.sponsorName || `Membro #${link.sponsorId}`}</p></div><span className={link.status === "active" ? "rounded-full bg-emerald-400/10 px-3 py-1 text-xs text-emerald-200" : "rounded-full bg-zinc-700/50 px-3 py-1 text-xs text-zinc-300"}>{link.status === "active" ? "Ativo" : "Arquivado"}</span></div><div className="mt-3 border-t border-white/10 pt-3"><p className="text-sm text-zinc-400">Indicado</p><p className="font-medium text-white">{link.referredName || `Membro #${link.referredUserId}`}</p></div></article>)}</div> : <p className="mt-4 rounded-xl border border-dashed border-white/15 p-5 text-sm leading-6 text-zinc-400">Nenhum vínculo cadastrado. Use o formulário para relacionar os membros de forma administrável.</p>}</section></section></main></DashboardLayout>;
}
