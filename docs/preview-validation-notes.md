

## Validação após a correção

A Home foi montada corretamente no preview. O DOM público confirma o item `Entrar` dentro de `.nav-links`, apontando para `/acesso`. O teste de navegador em viewport de 390px confirmou que o menu hambúrguer abre, o item fica visível e as três imagens estratégicas aparecem depois da copy da respectiva seção. O teste visual existente passou em desktop (1440px), tablet (1024px) e mobile (390px), sem overflow horizontal, com banner fechável e as três imagens carregadas.
